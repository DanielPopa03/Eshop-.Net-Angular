import { CheckedOutItemDto } from './checked-out-item-dto';

describe('CheckedOutItemDto', () => {
  it('should create an instance', () => {
    expect(new CheckedOutItemDto()).toBeTruthy();
  });
});
